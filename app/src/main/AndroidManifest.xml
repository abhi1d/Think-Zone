<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhi.thinkzone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Interruption" android:label="interruption">

        </activity>
        <activity android:name=".Login" android:label="Login">
        </activity>
        <activity android:name=".Home" android:label="Home">

        </activity>
       <activity android:name=".intermediate" >

       </activity>
      <activity android:name=".Teacher" android:label="Teachers">

      </activity>

    </application>

</manifest>